# Desktop Node Runtime

This directory is reserved for the portable Node.js runtime used by the packaged desktop build.

Expected layout:

```text
src-tauri/resources/node/windows-x64/node.exe
```

Populate it with:

```bash
npm run desktop:node:vendor
```
