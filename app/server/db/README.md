# Database Workspace

This directory holds the server-side database integration layers.

Planned layout:

- `client.js`: shared database client/bootstrap
- `schema/`: table definitions and query helpers
- `migrations/`: ordered SQL migrations

The database plan is documented in [`docs/database-architecture-plan.md`](/d:/UGit/resume-interview-workbench/docs/database-architecture-plan.md).
