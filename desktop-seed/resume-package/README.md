This directory is the desktop workspace resume package placeholder.

The portable desktop build intentionally does not ship with any personal resume data.

Expected user-managed files:

- `resume.json`
- `resume.meta.json`
- `resume.schema.json` (optional but recommended)

This workspace is initialized under LocalAppData and is safe to replace with your own resume package files.
